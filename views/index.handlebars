<h1>burgers!</h1>

<h2>Borgers that have yet to be served</h2>

<ul>
  {{#each burgers}}
    {{#unless served}}
      {{> burgers/burger-block}}
    {{/unless}}
  {{/each}}
</ul>

<h2>burgers that are going to be devoured</h2>
<ul>
  {{#each burgers}}
    {{#unless devoured}}
      {{#if served}}
        {{> burgers/burger-block}}
      {{/if}}
    {{/unless}}
  {{/each}}
</ul>

<h2>Add a burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="brgr">burger Name:</label>
    <input type="text" id="brgr" name="name">
  </div>
  <button type="submit">Add burger</button>
</form>